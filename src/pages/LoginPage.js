import AuthCard from '../components/AuthCard';

function LoginPage() {
  return (
    <main className="page-frame auth-page">
      <AuthCard mode="login" />
    </main>
  );
}

export default LoginPage;
